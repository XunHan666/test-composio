function text() {
  return { type: "text" };
}
function number() {
  return { type: "number" };
}
function checkbox() {
  return { type: "checkbox" };
}
function date() {
  return { type: "date" };
}
function timestamp() {
  return { type: "timestamp" };
}
function json() {
  return { type: "json" };
}
function select(selectOptions) {
  return { type: "select", ...selectOptions === void 0 ? {} : { selectOptions } };
}
function multiSelect(selectOptions) {
  return { type: "multi_select", ...selectOptions === void 0 ? {} : { selectOptions } };
}
const arrayTypeByElement = {
  text: "array_text",
  number: "array_number",
  checkbox: "array_checkbox",
  date: "array_date",
  timestamp: "array_timestamp"
};
function array(element) {
  return { type: arrayTypeByElement[element.type] };
}
function serializeXldbSchema(schema) {
  return {
    columns: schema.columns.map(({ name, title, type }) => ({ name, title, ...type }))
  };
}
export {
  array,
  checkbox,
  date,
  json,
  multiSelect,
  number,
  select,
  serializeXldbSchema,
  text,
  timestamp
};
