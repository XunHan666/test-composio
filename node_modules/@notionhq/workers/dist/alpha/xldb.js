function createXldb(name, config) {
  if (!name || typeof name !== "string") {
    throw new Error("XLDB name must be a non-empty string");
  }
  return { _tag: "xldb", name, config };
}
export {
  createXldb
};
